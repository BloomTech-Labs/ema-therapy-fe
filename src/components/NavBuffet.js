import React from 'react';

const NavBuffet = () => {
  return (
    <div>
      <button>Add Mood</button>
      <button>Mood</button>
      <button>Stories</button>
      <button>Stats</button>
      <button>Settings</button>
    </div>
  );
};

export default NavBuffet;
